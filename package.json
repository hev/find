{
  "name": "hev-ask",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@10.29.3",
  "scripts": {
    "dev": "pnpm --filter playground dev",
    "build": "pnpm --filter playground build",
    "kg:build": "pnpm --filter playground kg:build",
    "kg:verify": "pnpm --filter playground kg:verify",
    "build:npm-binaries": "node scripts/build-npm-binaries.mjs",
    "test": "pnpm --filter @hev/ask test",
    "typecheck": "pnpm -r typecheck"
  },
  "devDependencies": {
    "typescript": "^6.0.3"
  }
}
