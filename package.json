{
  "name": "hev-ask",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@10.29.3",
  "scripts": {
    "dev": "pnpm --filter playground dev",
    "build": "pnpm --filter playground build",
    "digest:build": "pnpm --filter playground digest:build",
    "digest:verify": "pnpm --filter playground digest:verify",
    "build:npm-binaries": "node scripts/build-npm-binaries.mjs",
    "test": "pnpm --filter @hevmind/ask test",
    "typecheck": "pnpm -r typecheck"
  },
  "devDependencies": {
    "typescript": "^6.0.3"
  }
}
