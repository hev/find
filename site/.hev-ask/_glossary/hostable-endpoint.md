---
term: "hostable endpoint"
aliases: ["standalone endpoint","/api/ask","search endpoint"]
---

The /api/ask route contract — keyword JSON, agentic SSE, and keyless digest reads — served by the Astro integration or deployed standalone (Worker, Node, Vercel) for other frameworks.
