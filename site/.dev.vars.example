# Copy to .dev.vars (gitignored) for local `astro dev` / `wrangler pages dev`.
# Enables the agentic /api/find path; without it, search degrades to keyword-only.
# In production, set the real value with:
#   wrangler pages secret put ANTHROPIC_API_KEY --project-name=hev-find
ANTHROPIC_API_KEY=
